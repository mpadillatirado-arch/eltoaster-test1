# Supabase publishable (anon) credentials.
#
# These are safe to commit: the publishable key is designed for browser use and
# is embedded in the built JS bundle regardless. Access is controlled by Row
# Level Security on the `leads` table, which allows INSERT only — anonymous
# reads return an empty set.
#
# To rotate or move these into Vercel's dashboard, set the same two names under
# Project Settings > Environment Variables; those values take precedence.
VITE_SUPABASE_URL=https://oichxzrvbvauazvvysll.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_uw4mXXj8bOXhSrvkHSIJcQ_GrUoBnwz
